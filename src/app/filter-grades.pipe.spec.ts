import { FilterGradesPipe } from './filter-grades.pipe';

describe('FilterGradesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterGradesPipe();
    expect(pipe).toBeTruthy();
  });
});
